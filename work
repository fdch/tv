<script>
function getContent() {
	jQuery.get('content.txt', function(data){
		lines = data.split("\n");
		$.each(lines, function(n, elem) {
			$("work").append('<div>' + elem + '</div>');
		});
	});
}
$(getContent());
</script>